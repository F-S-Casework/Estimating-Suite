---
phase: 03-live-data-layer
plan: 04
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - project/views-home.jsx
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04

must_haves:
  truths:
    - "PipelineView loads bids from Supabase on mount and shows a spinner while loading"
    - "Bids are grouped into kanban columns by their DB stage value"
    - "Clicking a kanban card calls onOpenBid(bid.id, bid.name) to open Estimator"
    - "A create-bid inline form appears at the top of the ITB column when triggered"
    - "Creating a bid with required fields (gc_name, name, due_date, project_type) persists to Supabase and appears in the kanban"
    - "Each card has a stage-advance button; clicking it updates bids.stage in Supabase"
    - "Won/Lost terminal stage shows Mark Won + Mark Lost buttons"
    - "HomeView hardcoded arrays remain as-is (HomeView is static; only PipelineView goes live)"
  artifacts:
    - path: "project/views-home.jsx"
      provides: "PipelineView with live Supabase data, create-bid form, stage advance"
      contains: "onOpenBid"
  key_links:
    - from: "PipelineView"
      to: "window.dbHelpers.getBids"
      via: "useEffect on mount"
      pattern: "getBids"
    - from: "create-bid form"
      to: "window.dbHelpers.addBid"
      via: "form submit handler"
      pattern: "addBid"
    - from: "stage-advance button"
      to: "window.dbHelpers.updateBidStage"
      via: "onClick handler"
      pattern: "updateBidStage"
---

<objective>
Replace PipelineView's hardcoded bid arrays with live Supabase data. Add the inline create-bid form and stage-advance buttons. Wire the onOpenBid prop so clicking a card navigates to the Estimator.

Purpose: Satisfies PIPE-01 (create bid), PIPE-02 (advance stage), PIPE-03 (live data), PIPE-04 (open Estimator on click).
Output: views-home.jsx with a fully live PipelineView. HomeView remains static (sample data acceptable per ROADMAP phase 3 scope).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-live-data-layer/03-CONTEXT.md
@.planning/phases/03-live-data-layer/03-RESEARCH.md
@.planning/phases/03-live-data-layer/03-UI-SPEC.md

<interfaces>
<!-- Current PipelineView structure (views-home.jsx lines 175–253) -->
<!-- Has hardcoded cols array with 5 columns: lead, qty, price, qc, sent -->
<!-- Must replace with 6 DB-stage columns: ITB, Takeoff/Pricing, Review, Submit, Won, Lost -->

<!-- DB stage → display label mapping (from 03-RESEARCH.md section 9) -->
const STAGE_COLS = [
  { id: 'ITB',             label: 'Lead / ITB Received' },
  { id: 'Takeoff/Pricing', label: 'Quantities & Pricing' },
  { id: 'Review',          label: 'Review / QC' },
  { id: 'Submit',          label: 'Submitted' },
  { id: 'Won',             label: 'Won' },
  { id: 'Lost',            label: 'Lost' },
];

<!-- React data fetching pattern (from 03-RESEARCH.md section 3) -->
const [bids, setBids] = useState(null);   // null = loading, [] = empty
useEffect(() => {
  let cancelled = false;
  async function load() {
    const { data, error } = await window.dbHelpers.getBids();
    if (!cancelled) {
      if (error) setError(error.message);
      else setBids(data || []);
    }
  }
  load();
  return () => { cancelled = true; };
}, []);

if (bids === null) return <window.Spinner />;

<!-- IMPORTANT: views-home.jsx uses no React hook aliases — uses window.Views global pattern -->
<!-- PipelineView is a const arrow function, NOT a named function declaration -->
<!-- Must add useState/useEffect to the file — they are NOT destructured at top of file -->
<!-- Add: const { useState: uS_home, useEffect: uE_home, useMemo: uM_home } = React; -->
<!-- at the top of the file (before HomeView definition) -->

<!-- onOpenBid prop received by PipelineView from shell.jsx (03-03-PLAN) -->
<!-- PipelineView signature: const PipelineView = ({ onOpenBid }) => { ... } -->
<!-- Card click: onClick={() => onOpenBid(bid.id, bid.name)} -->

<!-- Stage advance button per UI-SPEC: -->
<!-- Next stage button: <button className="btn ghost sm" style={{fontSize:11}} onClick={() => handleAdvance(bid)}>→ {nextStage}</button> -->
<!-- Won/Lost terminal: <button className="chip ok" onClick={() => handleStage(bid.id, 'Won')}>Mark Won</button> -->
<!--                    <button className="chip bad" onClick={() => handleStage(bid.id, 'Lost')}>Mark Lost</button> -->

<!-- Create-bid inline form per UI-SPEC and D-07/D-08 -->
<!-- Fields: gc_name (Client/GC), name (Project Name), due_date, project_type -->
<!-- Required fields — all 4 must be filled before submit is enabled -->
<!-- Appears as a .card at top of ITB column when showCreate is true -->
<!-- Triggered by: "Ingest ITB" button (already in shell topbar actions navigating to pipeline) -->
<!--               OR a "+" button in the ITB column header -->

<!-- File size note: views-home.jsx is 253 lines. -->
<!-- PipelineView rewrite will be substantial. Keep HomeView unchanged (static). -->
<!-- PipelineView alone will be ~100-130 lines. Total file ~250-280 lines — OK. -->
<!-- If approaching limit, write in two passes using Edit to append remaining. -->

<!-- window.dbHelpers.STAGE_NEXT for next-stage lookup -->
<!-- STAGE_NEXT = { 'ITB':'Takeoff/Pricing', 'Takeoff/Pricing':'Review', 'Review':'Submit', 'Submit':null } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite PipelineView with live data, create-bid form, stage advance</name>
  <read_first>
    - project/views-home.jsx (read full file — understand current HomeView and PipelineView structure, preserve HomeView unchanged)
  </read_first>
  <files>project/views-home.jsx</files>
  <action>
    Read the full views-home.jsx file. Keep HomeView (lines 1–172) exactly as-is. Replace only the PipelineView section (lines 175–253) and the window.Views registration.

    Add React hook aliases at the very top of the file (before the HomeView const):
    ```js
    const { useState: uS_home, useEffect: uE_home, useMemo: uM_home } = React;
    ```

    Replace the PipelineView const with this implementation:

    ```jsx
    const STAGE_COLS = [
      { id:'ITB',             label:'Lead / ITB Received' },
      { id:'Takeoff/Pricing', label:'Quantities & Pricing' },
      { id:'Review',          label:'Review / QC' },
      { id:'Submit',          label:'Submitted' },
      { id:'Won',             label:'Won' },
      { id:'Lost',            label:'Lost' },
    ];

    const PipelineView = ({ onOpenBid }) => {
      const [bids, setBids]           = uS_home(null);
      const [error, setError]         = uS_home(null);
      const [showCreate, setShowCreate] = uS_home(false);
      const [form, setForm]           = uS_home({ gc_name:'', name:'', due_date:'', project_type:'' });
      const [saving, setSaving]       = uS_home(false);

      uE_home(() => {
        let cancelled = false;
        async function load() {
          const { data, error: err } = await window.dbHelpers.getBids();
          if (!cancelled) {
            if (err) setError(err.message);
            else setBids(data || []);
          }
        }
        load();
        return () => { cancelled = true; };
      }, []);

      const bidsForStage = uM_home(() => {
        if (!bids) return {};
        const m = {};
        STAGE_COLS.forEach(c => { m[c.id] = []; });
        bids.forEach(b => { if (m[b.stage]) m[b.stage].push(b); });
        return m;
      }, [bids]);

      async function handleCreate(e) {
        e.preventDefault();
        if (!form.gc_name || !form.name || !form.due_date || !form.project_type) return;
        setSaving(true);
        const { data: newBid, error: err } = await window.dbHelpers.addBid(form);
        setSaving(false);
        if (err) { alert('Error creating bid: ' + err.message); return; }
        setBids(prev => [newBid, ...(prev || [])]);
        setForm({ gc_name:'', name:'', due_date:'', project_type:'' });
        setShowCreate(false);
      }

      async function handleAdvance(bid) {
        const next = window.dbHelpers.STAGE_NEXT[bid.stage];
        if (!next) return;
        const { error: err } = await window.dbHelpers.updateBidStage(bid.id, next);
        if (err) { alert('Error: ' + err.message); return; }
        setBids(prev => prev.map(b => b.id === bid.id ? { ...b, stage: next } : b));
      }

      async function handleTerminal(bid, stage) {
        const { error: err } = await window.dbHelpers.updateBidStage(bid.id, stage);
        if (err) { alert('Error: ' + err.message); return; }
        setBids(prev => prev.map(b => b.id === bid.id ? { ...b, stage } : b));
      }

      if (bids === null && !error) return <window.Spinner />;
      if (error) return (
        <div style={{padding:40, color:'var(--bad)'}}>
          Something went wrong — check your connection and try again.
          <button className="btn ghost sm" style={{marginLeft:12}} onClick={() => { setError(null); setBids(null); }}>Retry</button>
        </div>
      );

      const totalBids = bids.length;

      return (
        <div className="view active">
          <div className="page-head">
            <div>
              <div className="page-title">Pipeline</div>
              <div className="page-sub">{totalBids} {totalBids === 1 ? 'bid' : 'bids'} tracked</div>
            </div>
            <div className="spacer"></div>
            <div className="actions">
              <button className="btn accent" onClick={() => setShowCreate(true)}><Icon.plus/> New bid</button>
            </div>
          </div>

          <div style={{padding:'16px 20px 40px', overflowX:'auto'}}>
            {totalBids === 0 && !showCreate ? (
              <window.EmptyState
                heading="No bids yet"
                body="Create your first bid to start tracking the pipeline."
                action={{ label:'New Bid', onClick:() => setShowCreate(true) }}
              />
            ) : (
              <div className="kan">
                {STAGE_COLS.map(col => {
                  const colBids = bidsForStage[col.id] || [];
                  const isITB = col.id === 'ITB';
                  return (
                    <div className="col" key={col.id}>
                      <div className="col-head">
                        <span>{col.label}</span>
                        <span className="n">{colBids.length}</span>
                        {isITB && <button className="btn ghost sm" style={{marginLeft:'auto',fontSize:11}} onClick={() => setShowCreate(v => !v)}><Icon.plus/></button>}
                      </div>

                      {isITB && showCreate && (
                        <form className="card" style={{padding:'12px 14px',borderColor:'var(--accent)',borderWidth:1.5,marginBottom:8}} onSubmit={handleCreate}>
                          <div style={{fontSize:10.5,fontWeight:700,color:'var(--mute)',textTransform:'uppercase',letterSpacing:'.04em',marginBottom:3}}>Client / GC</div>
                          <input required value={form.gc_name} onChange={e=>setForm(f=>({...f,gc_name:e.target.value}))} placeholder="Turner Construction" style={{width:'100%',border:'1px solid var(--line)',borderRadius:'var(--r-sm)',padding:'5px 8px',fontSize:12.5,background:'var(--paper)',marginBottom:6}}/>
                          <div style={{fontSize:10.5,fontWeight:700,color:'var(--mute)',textTransform:'uppercase',letterSpacing:'.04em',marginBottom:3}}>Project Name</div>
                          <input required value={form.name} onChange={e=>setForm(f=>({...f,name:e.target.value}))} placeholder="Caldwell Medical Center" style={{width:'100%',border:'1px solid var(--line)',borderRadius:'var(--r-sm)',padding:'5px 8px',fontSize:12.5,background:'var(--paper)',marginBottom:6}}/>
                          <div style={{fontSize:10.5,fontWeight:700,color:'var(--mute)',textTransform:'uppercase',letterSpacing:'.04em',marginBottom:3}}>Bid Due Date</div>
                          <input required type="date" value={form.due_date} onChange={e=>setForm(f=>({...f,due_date:e.target.value}))} style={{width:'100%',border:'1px solid var(--line)',borderRadius:'var(--r-sm)',padding:'5px 8px',fontSize:12.5,background:'var(--paper)',marginBottom:6}}/>
                          <div style={{fontSize:10.5,fontWeight:700,color:'var(--mute)',textTransform:'uppercase',letterSpacing:'.04em',marginBottom:3}}>Project Type</div>
                          <input required value={form.project_type} onChange={e=>setForm(f=>({...f,project_type:e.target.value}))} placeholder="Healthcare TI" style={{width:'100%',border:'1px solid var(--line)',borderRadius:'var(--r-sm)',padding:'5px 8px',fontSize:12.5,background:'var(--paper)',marginBottom:8}}/>
                          <div style={{display:'flex',gap:6}}>
                            <button className="btn accent sm" type="submit" disabled={saving}>{saving?'Saving…':'Create Bid'}</button>
                            <button className="btn ghost sm" type="button" onClick={()=>setShowCreate(false)}>Cancel</button>
                          </div>
                        </form>
                      )}

                      {colBids.map((bid) => {
                        const nextStage = window.dbHelpers.STAGE_NEXT[bid.stage];
                        const isTerminal = bid.stage === 'Submit';
                        return (
                          <div key={bid.id} className="card-lead" onClick={() => onOpenBid && onOpenBid(bid.id, bid.name)} style={{cursor:'pointer'}}>
                            <div style={{fontWeight:600,fontSize:13,marginBottom:2}}>{bid.name}</div>
                            <div className="meta" style={{fontSize:11.5,marginBottom:6}}>{bid.gc_name} · {bid.project_type || '—'}</div>
                            <div style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginTop:4}}>
                              <span className="muted" style={{fontSize:11}}>{bid.due_date ? `Due ${bid.due_date}` : '—'}</span>
                              <div style={{display:'flex',gap:4}} onClick={e => e.stopPropagation()}>
                                {bid.stage !== 'Won' && bid.stage !== 'Lost' && (
                                  nextStage
                                    ? <button className="btn ghost sm" style={{fontSize:11,padding:'2px 6px'}} onClick={() => handleAdvance(bid)}>→ {nextStage}</button>
                                    : <>
                                        <button className="chip ok" style={{cursor:'pointer'}} onClick={() => handleTerminal(bid, 'Won')}>Mark Won</button>
                                        <button className="chip bad" style={{cursor:'pointer'}} onClick={() => handleTerminal(bid, 'Lost')}>Mark Lost</button>
                                      </>
                                )}
                              </div>
                            </div>
                          </div>
                        );
                      })}
                      {colBids.length === 0 && !isITB && (
                        <div style={{padding:'12px 8px',fontSize:12,color:'var(--mute)',textAlign:'center'}}>—</div>
                      )}
                    </div>
                  );
                })}
              </div>
            )}
          </div>
        </div>
      );
    };
    ```

    Keep the existing window.Views registration at the bottom unchanged:
    `window.Views = Object.assign(window.Views || {}, { home: HomeView, pipeline: PipelineView });`

    IMPORTANT: If file approaches 300 lines, write in two passes:
    - First Write: lines 1 through HomeView end (preserve unchanged) + React aliases + STAGE_COLS constant
    - Second Edit: append PipelineView implementation + window.Views registration
  </action>
  <verify>
    grep "onOpenBid" project/views-home.jsx
    grep "getBids" project/views-home.jsx
    grep "addBid" project/views-home.jsx
    grep "updateBidStage" project/views-home.jsx
    grep "STAGE_COLS" project/views-home.jsx
    grep "uS_home\|uE_home\|uM_home" project/views-home.jsx
  </verify>
  <done>
    - views-home.jsx top has const { useState: uS_home, useEffect: uE_home, useMemo: uM_home } = React
    - PipelineView uses uS_home/uE_home/uM_home (not useState/useEffect/useMemo directly)
    - PipelineView accepts onOpenBid prop and calls it with (bid.id, bid.name) on card click
    - Bids load from window.dbHelpers.getBids() with null-initial loading state
    - STAGE_COLS array uses exact DB stage values: 'ITB','Takeoff/Pricing','Review','Submit','Won','Lost'
    - Create-bid form has 4 required fields and calls window.dbHelpers.addBid
    - Stage advance buttons use window.dbHelpers.updateBidStage
    - HomeView is unchanged from original
    - File is under 300 lines (or was split at a component boundary if over)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User form input → addBid | gc_name, name, project_type are free text; passed to Supabase insert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | create-bid form inputs | accept | All inputs are plain text stored in a non-executable text column; Supabase parameterised queries prevent SQL injection; RLS limits to authenticated users |
| T-03-04-02 | Denial of Service | rapid stage-advance clicks | accept | Low-volume internal tool (4-10 users); no rate limiting needed at this scale |
| T-03-04-03 | Information Disclosure | bid names/GC names in kanban | accept | App is authenticated-only; all users are trusted employees |
</threat_model>

<verification>
Manual verification in browser:
1. Navigate to Pipeline → spinner shows briefly → kanban renders with DB bids (or empty state)
2. Click "+ New bid" or "+" in ITB column → inline form appears
3. Fill all 4 fields, click "Create Bid" → new card appears in ITB column, form closes
4. Click stage-advance button on a card → card moves to next column instantly
5. Advance a Submit-stage card → "Mark Won" and "Mark Lost" appear
6. Click a pipeline card → Estimator view opens with that bid's name in topbar crumb
</verification>

<success_criteria>
- PipelineView shows live Supabase bids grouped by DB stage in 6 kanban columns
- Spinner while loading (bids === null), EmptyState when bids.length === 0
- Create-bid form persists a new bid with gc_name, name, due_date, project_type, stage='ITB'
- Stage advance updates bids.stage in Supabase; card re-renders in new column
- Card click calls onOpenBid(bid.id, bid.name)
- HomeView is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-live-data-layer/03-04-SUMMARY.md`
</output>
